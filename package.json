{
  "name": "nail-salon-booking-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run start --prefix Front-end"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "mailer": "^0.6.7",
    "mongoose": "^8.16.0",
    "node": "^22.17.0",
    "nodemailer": "^7.0.3"
  }
}
